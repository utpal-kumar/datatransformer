1. Set the data files directory in app.config where *.Column.txt and *.txt files exist e.g. 
	abc.Columns.txt is column names containing file and abc.txt is the data file.
2. If merging temp files are not required to merge. (save 20 min per 200k lines) comment function used: MergeTemDataFilesIntoOne
3. 